# Credo configuration for bot_army_gtd

%{
  configs: [
    %{
      name: "default",
      strict: true,
      files: %{
        included: ["lib/"],
        excluded: []
      },
      checks: []
    }
  ]
}
